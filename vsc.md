# Using git via VSC

1. Review changes before committing: open source control -> click modified file -> review {file} changes / review {file} diff and suggest improvements.

2. Stage and commit changes by selecting the + icon next to each file or stage all changes at once. Type your commit message in the input box or use select the sparkle icon to use AI to generate the message.